<h1>Profile Items</h1>
<% @profile_items.each do |item| %>
  <p><%= item.title %></p>
  <p><%= item.subtitle %></p>
  <p><%= item.body %></p>
  <p><%= image_tag(item.thumb_image) %></p>
  <hr>
<% end %>
